import React from 'react'
import { storiesOf } from '@storybook/react/dist/client/preview';

import AlienIcon from './'

storiesOf('Components/AlienIcon', module)
  .add('default', () => (
    <AlienIcon />
  ))
